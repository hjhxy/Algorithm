impl Solution {
    pub fn find_anagrams(s: String, p: String) -> Vec<i32> {
        let mut p = p.chars().collect::<Vec<char>>();
        p.sort();
        for i in 0..s.len() {}
    }
}

fn main() {}

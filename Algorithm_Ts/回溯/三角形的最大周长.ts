function largestPerimeter(nums: number[]): number {
    for (let i = 0; i <nums.length-2; ++i) {
        for (let j = i+1; j < nums.length-1;j++){
            for (let z = j+1; z < nums.length;z++){
                if(nums[z]+)
            }
        }
    }
};


const res = largestPerimeter([1,2,1,10]);
console.log(res);

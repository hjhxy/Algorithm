function maxTaskAssign(tasks: number[], workers: number[], pills: number, strength: number): number {
    return 0;
};


const res = maxTaskAssign([3,2,1], [0,3,3], 1, 1);

console.log(res);
export {}